
(function(BRACKET) {

  var ready = function() {
    var region;
    for (var regionName in BRACKET.regions) {
      region = BRACKET.regions[regionName];
      
    }
  };

  $(ready);

})(BRACKET);